thumbor:
  build: thumbor
  volumes:
    - data:/data
    - logs:/logs
  ports:
    - "8000:80" # thumbor port
#  links:
#    - redis:redis
#remotecv:
#  build: remotecv
#  links:
#    - redis:redis
#redis:
#  image: redis:latest
